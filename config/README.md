# Config

Version-controlled configuration defaults and schemas. Runtime/user overrides should remain separate from deployable defaults and must not be overwritten by the updater unless explicitly classified for deployment.
